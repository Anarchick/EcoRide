{% extends 'base.html.twig' %}

{% block title %}EcoRide - Voyager{% endblock %}

{% block importmap %}
    {{ importmap(['app', 'scripts/specific/travel', 'travel/index.css']) }}
{% endblock %}

{% block main %}
<twig:TravelSearchBar :form="search_form" />

<div class="eco-container-main">
    <!-- Loading animation -->
    <div id="loading" class="htmx-indicator">
        <div class="d-flex flex-column justify-content-center align-items-center m-4">
            <div class="spinner-border text-success" role="status"></div>
            <span class="eco-small-text sr-only mt-2">Recherche en cours...</span>
        </div>
    </div>
    
    <!-- results area -->
    <div id="travel-results">
        <div class="text-center text-muted">
            <i class="bi bi-search"></i>
            <p class="eco-small-text">Utilisez la barre de recherche pour trouver des trajets</p>
        </div>
    </div>
</div>

{% endblock %}
