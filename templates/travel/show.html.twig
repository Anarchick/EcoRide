{% extends 'base.html.twig' %}

{% block title %}EcoRide - Voyager{% endblock %}

{% block main %}

<div class="container">
    <h1>Détails du trajet</h1>
    {# {{ dump(travel) }} #}
    <div class="border">
        <p>travel uuid : {{ travel.travelUuid }}</p>
        <p>usern uuid : {{ travel.driverUuid }}</p>
        <p>datetime : {{ travel.date.date}}</p>
        <p>fuelType : {{ travel.fuelType.value }}</p>
    </div>
</div>
{% endblock %}