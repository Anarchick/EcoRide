{
  "name": "ecoride",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts:watch": "tsc --watch",
    "build:prod": "tsc -p tsconfig.prod.json", 
    "dev": "browser-sync start --proxy localhost:8000 --files \"templates/**/*.twig,assets/**/*.{css,scss,js,ts},src/**/*.php\" --reload-delay 1000",
    "watch": "browser-sync start --proxy localhost:8000 --files \"templates/**/*.twig,assets/**/*.{css,scss,js,ts},src/**/*.php\" --no-open"
  },
  "keywords": [],
  "author": "Anarchick",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@types/node": "^24.0.12",
    "browser-sync": "^3.0.4",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.36.0"
  }
}
